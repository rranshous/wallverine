{
  "name": "wallverine",
  "version": "1.0.0",
  "description": "Voice-controlled wall animations",
  "main": "index.html",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "publish": "npm run build && cd dist && zip -r ../releases/wallverine-web-$(date +%Y%m%d-%H%M%S).zip * && cd .. && echo '✅ Build complete! ZIP created in releases/ folder' && echo '🚀 Starting preview server...' && npm run preview"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^4.4.0"
  }
}
